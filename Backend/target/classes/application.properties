# Spring Boot Application Configuration
# This is the main configuration file that sets the active profile

# Active Profile: dev (development) or prod (production)
# Change this to 'prod' before deploying to AWS
spring.profiles.active=prod

# Application Name
spring.application.name=samaysetu
