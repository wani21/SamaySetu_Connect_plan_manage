# Production Profile Configuration
# Active when: spring.profiles.active=prod

spring.application.name=samaysetu

# Production Database Configuration (Supabase)
# These will be overridden by environment variables in AWS
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.tehdpecquwvgwpombtbl.supabase.co:5432/postgres}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:samaysetumitaoe}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration for PostgreSQL
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Connection Pool Settings (HikariCP) - Optimized for production
spring.datasource.hikari.maximum-pool-size=20
spring.datasource.hikari.minimum-idle=10
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.leak-detection-threshold=60000

# JWT Configuration
jwt.secret.key=${JWT_SECRET_KEY:QUhTR0Q3NjhBU0Q2ODc2U0RHNzZTREc3UzZERzdTNkRHQUhTR0Q3NjhBU0Q2ODc2U0RHNzZTREc3UzZERzdTNkRH}

# Server Configuration
server.port=${PORT:8083}

# Email Configuration (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:202301040228@mitaoe.ac.in}
spring.mail.password=${EMAIL_PASSWORD:azvvsfjhzhvzdjkc}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# Email Sender Display Name
app.email.from-name=SamaySetu Admin

# Application Base URL (Production)
# This will be set via environment variable in AWS
app.base-url=${APP_BASE_URL:https://your-backend-url.amazonaws.com}

# Logging Configuration (Production - Less verbose)
logging.level.root=INFO
logging.level.com.College.timetable=INFO
logging.level.org.hibernate.SQL=WARN
logging.level.org.springframework.web=INFO

# Security Headers
server.error.include-message=always
server.error.include-binding-errors=never
server.error.include-stacktrace=never
server.error.include-exception=false
